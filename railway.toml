[build]
  builder = "NIXPACKS"
  buildCommand = "npm install --prefix frontend && cd frontend && CI=false npm run build"

[deploy]
  startCommand = "node backend/src/app.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
